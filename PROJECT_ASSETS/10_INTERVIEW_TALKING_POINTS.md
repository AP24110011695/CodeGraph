# Interview Talking Points

1. **Problem framing** — Codebases lack a durable architectural brain; search alone is insufficient.  
2. **Composition rule** — Every CG module must reuse engines; duplication is a design failure.  
3. **Copilot is orchestration, not another analyzer** — Planning decides; tools execute; providers synthesize.  
4. **Impact without a second BFS** — reuse Semantic traverser + GraphQuery.  
5. **Memory vs conversation** — Repository Memory is durable repo knowledge; Copilot conversation memory is session context.  
6. **Provider pattern** — Timeline history, cache, and LLM swap behind interfaces.  
7. **RC-1 honesty** — green tests + labeled mocks beat fake “production ready” claims.  
8. **AI_CONTEXT** — documentation as an engineering control plane for humans and agents.  
9. **Failure mode I fixed** — routers existed but weren’t registered → 18 red tests → registration + path helper.  
10. **What I’d build next** — auth, Redis, live Git providers, PR-driven impact.
